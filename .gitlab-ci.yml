---
image: golang:latest

variables:
  REPO_NAME: github.com/samsung-cnct/k2-crash-application

stages:
  - test
  - build

before_script:
  - export GOPATH=$(dirname $CI_PROJECT_DIR)/go
  - mkdir -p $GOPATH/src
  - cd $GOPATH/src
  - ln -s $CI_PROJECT_DIR
  - cd $CI_PROJECT_NAME
  - go version
  - go env
  - go get github.com/c4milo/github-release
  - go get github.com/mitchellh/gox 

test:
  stage: test
  script:
    - pwd
    - ls $CI_PROJECT_DIR
    - ls ./Makefile
    - make test

build:
  stage: build
  script:
    - pwd
    - ls -alt $GOPATH/bin/github.com
    - make compile

