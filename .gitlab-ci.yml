---
image: golang:latest

variables:
  REPO_NAME: github.com/samsung-cnct/k2-crash-application

stages:
  - test
  - build

before_script:
  - mkdir -p /go/src/github.com/samsung-cnct
  - ln -s $CI_PROJECT_DIR /go/src/github.com/samsung-cnct/k2-crash-application
  - cd /go/src/github.com/samsung-cnct/k2-crash-application
  - go version
  - go env
  - make deps

test:
  stage: test
  script:
    - pwd
    - ls -alt 
    - make test

build:
  stage: build
  script:
    - pwd
    - ls -alt $GOPATH/bin/
    - ls -alt 
    - make compile

